class Connection {
  constructor (leftKey, leftItem, rightKey, rightItem) {
    this.leftKey = leftKey;
    this.leftItem = leftItem;
    this.rightKey = rightKey;
    this.rightItem = rightItem;
    this.count = 1;
  }
}
export default Connection;
